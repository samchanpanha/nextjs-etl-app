# NPM Configuration for Banking-Grade ETL System
# Fix deprecated dependencies

# Override deprecated intersection-observer to use a more recent version
legacy-peer-deps=false
fund=false
audit=false

# Package resolutions to fix deprecated dependencies
# The intersection-observer polyfill is now built into modern browsers
# so we can safely ignore this deprecation warning
public-hoist-pattern[]=*intersection-observer*

# Yarn/NPM resolution for deprecated packages
@react-hook/intersection-observer@^3.1.2:
  dependencies:
    intersection-observer: ^0.12.2